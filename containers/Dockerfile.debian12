# KubeXM Build Container - Debian 12 (Bookworm)
# Purpose: Build DEB packages and local repository for Debian 12

FROM debian:12

LABEL maintainer="KubeXM Team"
LABEL description="Debian 12 Bookworm build environment for KubeXM offline packages"
LABEL os.type="debian"
LABEL os.version="12"
LABEL pkg.manager="apt"

# Set non-interactive mode
ENV DEBIAN_FRONTEND=noninteractive

# Update package lists and install build tools
RUN apt-get update && apt-get install -y \
    apt-rdepends \
    dpkg-dev \
    wget \
    curl \
    tar \
    gzip \
    xz-utils \
    gnupg \
    apt-transport-https \
    ca-certificates \
    && apt-get clean \
    && rm -rf /var/lib/apt/lists/*

# Create build directories
RUN mkdir -p /build/packages /build/repo /build/output

WORKDIR /build

# Copy build scripts
COPY scripts/build-deb.sh /build/
RUN chmod +x /build/build-deb.sh

# Default command
CMD ["/bin/bash"]
