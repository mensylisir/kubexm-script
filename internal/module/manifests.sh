#!/usr/bin/env bash
set -euo pipefail

# ==============================================================================
# Manifests Module
# ==============================================================================
# 清单显示模块
# ==============================================================================

source "${KUBEXM_ROOT}/internal/task/common.sh"
source "${KUBEXM_ROOT}/internal/task/manifests/main.sh"

# -----------------------------------------------------------------------------
# 显示清单
# -----------------------------------------------------------------------------
module::manifests() {
  local ctx="$1"
  shift
  task::manifests "${ctx}" "$@"
}

export -f module::manifests