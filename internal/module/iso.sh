#!/usr/bin/env bash
set -euo pipefail

# ==============================================================================
# ISO Module
# ==============================================================================
# 系统 ISO 构建模块
# ==============================================================================

source "${KUBEXM_ROOT}/internal/task/common.sh"
source "${KUBEXM_ROOT}/internal/task/iso/main.sh"

# -----------------------------------------------------------------------------
# 构建 ISO
# -----------------------------------------------------------------------------
module::iso_build() {
  local ctx="$1"
  shift
  task::iso_build "${ctx}" "$@"
}

export -f module::iso_build